<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns="http://xmlns.jcp.org/xml/ns/javaee"
	xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
	version="3.1">
	<display-name>study4_6</display-name>
	<welcome-file-list>
	<!-- 사용자가 입력하지 않으면 아래 경로상에서 찾음 -->
		<welcome-file>index.html</welcome-file>
		<welcome-file>index.htm</welcome-file>
		<welcome-file>index.jsp</welcome-file>
	</welcome-file-list>

	     
		
	<!-- 인코딩 필터 -->
	<filter>
		<filter-name>encodingFilter</filter-name>
		<filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
		<init-param>
			<param-name>encoding</param-name>
			<param-value>UTF-8</param-value>
		</init-param>
		<init-param>
			<param-name>forceEncoding</param-name>  
			<param-value>true</param-value>
		</init-param>
	</filter>
	<filter-mapping>
		<filter-name>encodingFilter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
	
	
	<!-- 루트 컨테이너 설정파일의 위치 -->
	<context-param>
		<param-name>contextConfigLocation</param-name>
		<param-value>classpath:spring/context-*.xml</param-value>
	</context-param>
	<!-- 루트 컨테이너 선언 -->
	<!-- <listener>
		<listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
	</listener> -->
	
	<!-- 웹(자식) 컨테이너 선언 -->
	<servlet>
		<servlet-name>monkey</servlet-name>
		<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
		<init-param>
			<param-name>contextConfigLocation</param-name>
			<param-value>/monkey/config/*.xml</param-value> <!-- monkey/config 아래 xml 전부 포함 -->
		</init-param>
		<load-on-startup>1</load-on-startup>
	</servlet>
	
	
		
	<!-- 클라이언트와 매핑
	     확장자 매핑 : *.nhn
	     경로매핑 : /board/*   ,   /* 
	     완전경로매핑 : /admin/list.soju 
	     기본서블릿매핑 : /  다른 서블릿 매핑을 제외한 요청을 처리
	         톰캣에 *.jsp 매핑이 걸려있습니다.
	     
	     -->
	
	<!-- 클라이언트와 매핑 -->	
	<servlet-mapping>
		<servlet-name>monkey</servlet-name>
		<url-pattern>*.wow</url-pattern>
	</servlet-mapping>
</web-app>